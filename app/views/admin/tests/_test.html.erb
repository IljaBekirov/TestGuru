<tr>
<!--  <td><%#= test.id %></td>-->
  <td><%= test.title %></td>
  <td><%= test_level(test.level) %></td>
  <td><%= find_category(test.category_id) %></td>
  <td><%= test.questions.count %></td>
  <td><%= link_to "Просмотр", admin_test_path(test) %></td>
  <td><%= link_to "Редактировать", edit_admin_test_path(test) %></td>
  <td><%= link_to "Удалить", admin_test_path(test), method: :delete, data: { confirm: "Вы уверены, что хотите удалить?" } %></td>
</tr>