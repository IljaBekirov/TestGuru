<table>
  <thead>
  <tr>
    <th>id</th>
    <th>Body</th>
    <th></th>
  </tr>
  </thead>

  <tbody>
  <% questions.each do |question| %>
    <tr>
      <td><%= question.id %></td>
      <td><%= question.body %></td>
      <td><%= link_to "Просмотр", test_question_path(@test, question) %> |</td>
      <td><%= link_to "Редактировать", edit_test_question_path(@test, question) %>  |</td>
      <td><%= link_to "Удалить", test_question_path(@test, question), method: :delete, data: { confirm: "Вы уверены, что хотите удалить?" } %></td>
    </tr>
  <% end %>
  </tbody>
</table>
